import openai
import os

openai.api_key = os.getenv("OPENAI_API_KEY")

  response = openai.api_requests.Image.create(
  prompt="a white siamese cat",
  n=1,
  size="256x256"
)
image_url = response.data[0].url
